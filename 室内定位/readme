在室内定位技术中做的室内几何定位和PDR内容
基本原理如下：
几何定位是一种基于严格数学推导的定位模式，在已知基站坐标的前提下，测量目标位置相对基站位置的距离或角度，通过建立数学模型求解目标位置。本次的算例基于距离交会模式，使用无人机、小推车等采集的UWB和超声波数据，
进行解算。
已知第i个测站在站心坐标系下东向、北向和垂向的坐标分别为Ei、Ni和Ui，目标位置到基站i的实际距离为di，目标位置在站心坐标系下东向、北向和垂向的坐标分别为Ed、Nd和Ud，根据勾股定理构建数学模型，进行线性化
进行最小二乘迭代解算即可。
若考虑传感器的设备零偏，则在每个测站的观测方程后面增加零偏参数即可。
对于室内定位，多数时候先确定楼层，然后使用二维定位。

航迹推算的思路是由上一时刻导航状态（位置、速度和姿态），结合传感器观测值，获得相对角运动和相对线运动，通过航迹推算算法获得当前的导航状态。本次报告以行人航迹推算算法PDR为例进行解算和分析。
PDR算法主要包括三个部分：人体航向估计、脚步探测和步长估计。航向角可以通过磁强计和陀螺数据计算。由磁强计数据计算航向角需要调平磁强计数据，即将其从三维投影到水平面，调平需要首先使用加速度计数据计算得到横滚角r和航向角
为了削弱观测数据误差的影响，对水平姿态角进行1s滑动平均，使用1s滑动平均后的横滚角和俯仰角进行后续计算。使用横滚角和俯仰角把磁强计测量数据从b系转化到n1系
磁北航向加上磁偏角D可以得到真北航向，磁偏角可以通过国际地磁参考场（IGRF）模型获得，
由陀螺数据计算航向角同样需要将陀螺测量值从b系转换到n1系，上一时刻的航向角加上对航向角变化率的积分可以得到下一时刻的航向角。
使用加速度数据进行脚步探测，计算三轴加速度数据的模长。
静止时三轴加速度模长约等于重力加速度，脚离地时失重，使加速度模长小于重力加速度，因此认为三轴加速度数据的波谷为脚步。步长的估计通常需要行走距离和探测到的步数训练模型，本次算例中使用固定步长。
已知上一历元的位置，两个历元之间的步长估计值和上一历元航向角，即可进行推算
